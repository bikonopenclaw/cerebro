Estado: GERAÇÃO NATIVA COMPROVADA; HANDOFF ESCOPO EXATO RESOLVIDO EM 08/09/2026 POR AUTORIZAÇÃO EXPRESSA DE HEBERT. O run original `ced39e0b-c501-4c60-8384-019958bf1dce` foi reconciliado e interrompido pelo controle do CLI proprietário; o turno nativo registrou `turn_aborted`. Não retomar nem repetir sua geração. Sete PNGs completos do thread `01a0813c-b8ba-7a61-864d-70849ea46469` foram validados e preservados. O piloto usa a fotografia existente sem headline/logo gerado, com finalização determinística autorizada. Revisão formal APROVADO e entrega Telegram messageId 794 confirmadas. Hebert confirmou “mensagem recebida, aprovada” na conversa original; este piloto está HUMAN_ACCEPTED / COMPLETE. Não gerar, reenviar nem publicar a partir desse aceite. Estado atual e hashes em `pilot-operational-receipt.md`; notas BLOCKED anteriores são histórico superado para este piloto.

A falha real é a fronteira de artefato: o handler instalado `recordNativeGeneratedMedia` registra `savedPath` no Codex home, mas não o exporta ao workspace legível pelo shell Robotnik. O handler de base64 não resolve este formato de retorno. Não existe ferramenta callable de exportação desse savedPath no schema observado. A negação anterior continua válida; esta resolução decorre de nova autorização expressa de Hebert, não de bypass.

Handoff específico: `scripts/handoff_existing_pilot.py`, execução única pelo proprietário/runtime autorizado, sem serviço novo, setuid, mount ou ferramenta de cópia disponível ao agente. Não aceita argumentos de caminho. Está congelado nos sete nomes, tamanhos e hashes de PNGs completos daquele thread; valida componentes sem symlink, arquivo regular único, estabilidade, limite 25 MiB, assinatura, CRC/IEND, dimensões e decode. Destino único: `entregas/piloto-contrato-criativo-v1-20260908/native-candidates/`. Reexecução só reconcilia bytes iguais, nunca substitui arte. Recibos completos de source/destination/hash/tamanho/dimensões/uid/gid/mode ficam no recovery privado. O Robotnik lê apenas as cópias autorizadas no próprio workspace. Outros threads não estão autorizados por esse importer: se o retorno futuro não tiver export nativo suportado, bloquear e registrar a dependência exata, sem gerar novamente ou ampliar roots.

Autenticação permitida: somente conta ChatGPT já autorizada do Robotnik, na identidade nativa do gateway existente. Nenhuma API key, CLI de imagem paga, outro provider, conector, root renderer ou ampliação de permissões é rota alternativa. A finalização local estrita de tipografia/logo/crop/cor/export do piloto existente foi expressamente autorizada por Hebert, preservando a fotografia nativa.

A rota principal única é `image_gen.imagegen`, ferramenta embutida do Codex/ChatGPT, anunciada no schema real da sessão Robotnik `01a08130-089d-7a10-ae3c-a5a7cfe144e1`, gateway run `a30f1349-aa82-49be-a923-abb208df95d4` em 08/09/2026. Consumir pelo code mode nativo, com `tools.image_gen__imagegen`, conforme schema anunciado. Não usar `openclaw.image_generate`: foi apenas inventariado, não é a rota principal registrada. Não gerar pela CLI de imagem.

O caminho host da skill técnica `.system/imagegen/SKILL.md` não está montado no filesystem de comandos do Robotnik. Isso não bloqueia a ferramenta embutida. A instrução técnica legível para esta rota está neste documento; não ficar tentando ler o caminho inacessível nem copiar credenciais. Nenhuma permissão do sandbox foi ampliada.

Operação: abrir primeiro a referência local com `view_image`; fornecer `referenced_image_paths` quando o schema e o acesso nativo admitirem todos os arquivos, ou `num_last_images_to_include` com o menor número de imagens já visíveis necessário. Nunca usar os dois campos juntos. O papel da imagem é referência de estilo, não alvo a copiar. Chamar uma vez para o único piloto; aguardar o mesmo handle com `wait` se a chamada ainda estiver em execução. Entregar o retorno ao runtime com `generatedImage(result)` quando aplicável. Capturar o resultado real e materializar somente pelo export nativo suportado ou handoff específico autorizado descrito acima para `entregas/piloto-contrato-criativo-v1-20260908/`. Não inventar argumento de destino nem fazer cópia ad hoc. O importer proprietário acima é exclusivo do thread e hashes autorizados. Se o artefato não for legível por mecanismo autorizado, registrar a falha de handoff e bloquear, sem ampliar roots.

A ferramenta embutida usa a autenticação ChatGPT já existente; não solicitar API key. Conteúdo, restrições e referência vêm do contrato integral. Deterministicamente aplicar o logo PNG oficial na finalização sem redesenho, mantendo alfa e proporção. Uma eventual mudança da ferramenta principal exige revisão explícita deste registro e aprovação de Hebert.

Para produção, carregar o contrato integral e insumos de brand-assets.json. A referência é entrada de geração/edição quando suportada e comparação visual obrigatória. Registrar nome exato da ferramenta, chamada, provider, sessão, referência de entrada, saída bruta e hashes separados. Não chamar ferramentas de publicação ou staging.

Finalização determinística permitida: tipografia, aplicação do PNG oficial transparente, recorte, contraste e exportação 1080 × 1350 sRGB. Não substitui a fotografia humana. Revisão Kowalski exige os arquivos reais por handoff restrito autorizado. Falta de acesso do revisor bloqueia a entrega final. Telegram usa apenas a diretiva MEDIA e a conversa autenticada descrita em TOOLS.md, após revisão.
