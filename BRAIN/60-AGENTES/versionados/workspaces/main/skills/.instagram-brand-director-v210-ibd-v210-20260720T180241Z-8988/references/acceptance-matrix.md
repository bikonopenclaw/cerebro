# Matriz de aceite 2.1.0

- frontmatter somente name e description;
- todos os arquivos e referências presentes;
- YAML, JSON e Python válidos;
- nenhum .pyc ou dependência legada;
- controles da v2.0.1 preservados;
- provider registry deny by default;
- seis tipos de provider definidos;
- providers externos desabilitados;
- Skipper, Rico e Private desabilitados;
- event stream append-only com hash chain, verify e replay;
- Asset Pipeline com checksum, lineage e transições;
- Governance Engine fail-closed;
- request e hash canônicos;
- OK vinculado a proprietário, evento, mensagem, ação, destino, provider, versão e payload;
- lifecycle approved, reserved, executing e outcome;
- resultado de adapter persistido em evento;
- Kling com argv tipado e shell desativado;
- download HTTPS com allowlist, limite e hash;
- publicação sem adapter bloqueada;
- composição bloqueada sem owner, ImageMagick, templates e Motion Canvas;
- comandos legados cobertos por testes;
- playbooks e plano de migração presentes;
- quick_validate, validate_skill, preflight build e suíte automatizada verdes.

Qualquer item ausente bloqueia a candidata.
