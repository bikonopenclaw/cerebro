# 2026-07-02

## Pessoas

- Felipe Nogueira: em relatórios operacionais externos, deve aparecer como solicitante/responsável junto de Hebert quando o pedido partir dele; nomes de agentes ou `Puppet Master` não devem aparecer no documento final.

## Empresas

- BIKON: o padrão de documentos externos foi reforçado para remover metadados de impressão, caminhos locais e referências internas de automação antes de enviar arquivos a cliente/grupo.

## Projetos

- Brain: snapshots versionados de Kowalski, Darth Vader e Robotnik sincronizados antes da consolidação; o script de snapshot foi endurecido para excluir diretórios temporários `tmp*`, evitando versionar artefatos de validação gerados por execução.
- Kowalski/Relatórios Operacionais: relatório EOL precisou de correção na causa para gerar PDF sem cabeçalho/rodapé automático do Chromium, porque o rodapé de impressão expunha caminho local do arquivo.
- Darth Vader/Notaas NFS-e: o lote Remessa 092 consolidou padrão mensal com emissão cadenciada, validação de XML/PDF antes de e-mail, geração de 1 boleto por NFS-e, 1 remessa CNAB400 por lote e e-mails agrupados por `cliente_id`.
- Controle Financeiro Familiar: pacote completo fase 4 foi produzido como ZIP limpo, sem `.env`, banco, uploads ou pendências antigas; também foi criado guia seguro de desinstalação com backup recomendado antes de apagar a pasta local.

## Oportunidades

## Problemas

- `memory_search` permaneceu indisponível por quota insuficiente do provedor de embeddings; consolidação baseada em sessões visíveis, arquivos locais do Brain, diffs Git e inspeção direta.
- O snapshot inicial trouxe diretórios temporários não rastreados da Darth Vader com dados de validação; eles não foram versionados e o script foi ajustado para impedir recorrência.

## Decisões

- Diretórios temporários (`tmp`, `tmp-*`, `tmp_*`, `temp`) não devem entrar nos snapshots versionados dos agentes, mesmo quando contêm JSON/HTML tecnicamente legíveis.
- PDFs externos gerados via navegador devem ser validados contra caminhos locais, `file://`, nomes de agentes e cabeçalho/rodapé automático antes do envio.

## Aprendizados

- A origem de vazamento de caminho local em PDF pode estar no mecanismo de impressão, não no HTML; a correção correta é gerar com header/footer desligado e validar o PDF final.
- Artefatos de dry-run podem conter dados operacionais reais ou sensíveis mesmo quando não contêm segredo; devem ficar fora do Brain/Git salvo versão sanitizada e intencional.

## Ideias

## Próximas ações

- Restaurar ou trocar o provedor de embeddings para recuperar `memory_search`.
- Manter auditoria de snapshots após cada sync para evitar inclusão acidental de artefatos temporários.
