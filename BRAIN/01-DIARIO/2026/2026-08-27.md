# 2026-08-27

## Pessoas

- Hebert concluiu o aceite real do [[50-PROJETOS/Em-Andamento/OpenClaw-Provimento-213|Portal 213]] no iPhone: as duas acoes legadas abriram corretamente e as pendencias carregaram com descricao.
- Felipe Nogueira recebeu autoridade operacional limitada ao grupo [[70-AUTOMACOES/RELATORIOS-OPERACIONAIS-TELEGRAM|Relatorios Operacionais]] para tickets NinjaOne, scripts NinjaOne ja existentes e previamente aprovados, alteracao de layout de relatorio e pesquisa read-only em API ja liberada. API/endpoint novo, criacao ou alteracao de script, cron/config/integracao, acao em massa, backup, comunicacao externa, financeiro, fiscal e gasto continuam fora do escopo.

## Empresas

- [[20-EMPRESAS/BIKON/README|BIKON]]: o CNS `02.133-7`/`021337` do Cartorio do I Oficio de Alfredo Chaves foi ratificado pelo Owner; JSON, CSV e SQLite permaneceram consistentes, e os cards cadastrais validados foram entregues sem refacao dos dados.

## Projetos

- [[50-PROJETOS/Em-Andamento/OpenClaw-Provimento-213|OpenClaw - Provimento 213]]: o primeiro aceite real do build `B20260826T174303Z` falhou porque o card fixado enviava parametros versionados do baseline anterior e o runtime aceitava apenas o sufixo do build atual. A causa foi fechada como `SIGNED_VERSIONED_START_PARAM_REJECTED_DURING_INITDATA_VALIDATION`.
- O repair build `B20260826T194616Z`, commit `f34df545c40556f747772f612931eaf1917b943b`, tree `b3d4808dca64798a8a8b2b35b90a8fe72bfb1cdb`, passou `266/266` testes, `6/6` serventias, `178/178` pendencias, compatibilidade dos dois botoes legados, autenticacao, autorizacao, replay/freshness, membership, isolamento de tenant e paridade loopback/Tailnet.
- A validacao independente corrigida do Kowalski fechou `PASS` e o aceite real posterior no iPhone fechou `PORTAL_213_DELIVERY=ACCEPTED`; o Goal `PORTAL_213_PORTFOLIO_WIDE_CANONICAL_PENDING_EVIDENCE_REPAIR` foi encerrado no build `B20260826T194616Z`.

## Oportunidades

- Reutilizar em futuras Mini Apps o gate de compatibilidade entre cards/deep links persistentes e parametros versionados aceitos pelo runtime antes de promover build.

## Problemas

- A primeira validacao independente do repair build falhou por apontar para o root `prov213-core`, que nao era repositorio Git. A repeticao foi limitada a corrigir a origem para o worktree autenticado, preservando o recibo de falha e a linhagem de evidencia.

## Decisões

- Manter o reparo no mesmo Goal e na mesma linhagem funcional; recuperacao de tmux/worktree nao foi tratada como projeto ou Goal novo.
- Considerar o Portal 213 aceito somente depois de validacao de maquina, validacao independente e teste real do Owner no iPhone, nesta ordem.

## Aprendizados

- Card Telegram fixado e versionado e parte do contrato de compatibilidade: um build novo precisa aceitar os parametros persistentes autorizados ou coordenar migracao explicita do card.
- Validacao independente deve autenticar tambem o root Git/worktree usado como fonte; caminho funcional correto pode ser raiz de repositorio incorreta e produzir falso bloqueio de infraestrutura.

## Ideias

- Incluir fixtures de parametros de cards/deep links ainda ativos na regressao obrigatoria de toda promocao de Mini App Telegram.

## Próximas ações

- Portal 213: preservar build, manifesto, recibos e evidencias do aceite; nenhuma acao do Owner permanece pendente neste Goal.
- Relatorios Operacionais: aplicar a autoridade de Felipe somente aos quatro dominios autorizados e manter todos os limites excluidos fail-closed.
